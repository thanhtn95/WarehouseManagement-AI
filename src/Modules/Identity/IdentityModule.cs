namespace Wms.Modules.Identity;

/// <summary>
/// Assembly anchor for the Identity module. Exists so architecture tests and
/// DI registration can locate this assembly without hard-coding its name.
/// </summary>
public static class IdentityModule;
